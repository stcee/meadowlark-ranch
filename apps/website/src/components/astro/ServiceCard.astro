---
/**
 * ServiceCard.astro
 * Service card with background image, overlay, title, and description.
 * Links to a detail page. Matches the original .service-card CSS.
 */

interface Props {
  title: string;
  description: string;
  image: string;
  href: string;
  textPosition?: 'top' | 'bottom';
}

const { title, description, image, href, textPosition = 'bottom' } = Astro.props;
---

<a href={href} class={`service-card${textPosition === 'top' ? ' service-card--text-top' : ''}`}>
  <img src={image} alt={title} class="service-card__bg" loading="lazy" />
  <div class="service-card__overlay"></div>
  <div class="service-card__content">
    <h3 class="service-card__title">{title}</h3>
    <p class="service-card__text">{description}</p>
  </div>
</a>
